. assertion

[[ -d results ]] || mkdir results

echo "Testing power"
cat data/power.dat | ../power > results/power.1

echo
assert 'diff expected/power.out results/power.1 &> /dev/null'

echo
echo "PASS: ${success_count}"
echo "FAIL: ${failure_count}"
